apiVersion: v1
kind: Service
metadata:
  name: processor-python-service
spec:
  selector:
    app: processor-python
  ports:
  - protocol: "TCP"
    port: 6000
    targetPort: 5000
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: processor-python
spec:
  selector:
    matchLabels:
      app: processor-python
  replicas: 4
  template:
    metadata:
      labels:
        app: processor-python
    spec:
      containers:
      - name: processor-python
        image: processor-python:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 5000